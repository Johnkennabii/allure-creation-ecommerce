# Configuration Backend Medusa
# Copiez ce fichier vers .env.local et remplissez les valeurs

# URL du backend Medusa (local ou production)
MEDUSA_BACKEND_URL=http://localhost:9000

# Clé publique Medusa (obtenue depuis le dashboard admin)
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_test

# URL du site frontend
NEXT_PUBLIC_BASE_URL=http://localhost:8000

# Région par défaut (ISO-2 code en minuscule: fr, us, uk, etc.)
NEXT_PUBLIC_DEFAULT_REGION=fr

# Clé publique Stripe (pour les paiements)
# Obtenue depuis https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_KEY=

# Clé publique Medusa Payments (optionnel)
NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY=

# ID du compte externe Medusa Payments (optionnel)
NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID=

# Secret pour la revalidation Next.js
REVALIDATE_SECRET=supersecret

# Configuration Cloud Medusa S3 (optionnel, pour les images)
MEDUSA_CLOUD_S3_HOSTNAME=
MEDUSA_CLOUD_S3_PATHNAME=
